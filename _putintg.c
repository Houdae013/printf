#include "main.h"
/**
 * _putintg- print intigers
 * @in: intiger
 * return n
 */

int _putintg(int *in)
{
	int n = 0, co = 0;
	if (in == NULL)
	if (in == 0)
		return 1;
	while (in != 0)
	{ 
		in /= 10;
		co++;
		n++
	}
	return n;
}
